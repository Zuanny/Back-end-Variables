let r = 2
let h = 12
let areaSuperficie = Math.PI * r * r
let areaTotal = areaSuperficie * 2 + areaSuperficie * h
console.log(
  `A area total de um cilindro com raio de ${r} metros e altura de ${h} metros e igual a ${areaSuperficie} Metros quadrados`
)
